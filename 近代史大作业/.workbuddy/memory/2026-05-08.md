# 2026-05-08 工作日志

## 长征数字地图第二轮升级
- 故事内容更新：6个感人故事（半条被子、彝海结盟、七根火柴、金色鱼钩、陈树湘断肠明志、丰碑）
- 背景缩放同步：新增 `#map-background` 层，跟随SVG同步缩放变换
- 交互灵敏度提升：
  - 缩放范围调整为 0.4x ~ 3x
  - 滚轮缩放因子从 1.12/0.89 提升到 1.15/0.87
  - 拖拽阈值从 5px 降低到 3px
  - 节点增加 25px 透明点击区域（hit area）
- 节点 hover 效果增强

## 长征数字地图升级
- 修改地图背景：星空背景改为山水风景图片 + 半透明遮罩
- 新增"长征故事"模态框（点击顶部📜按钮）：
  - 关键数据统计（2年、600+战斗、25000里、4支红军）
  - 6个故事卡片，带图片和详细文字
- 新增"长征精神"模态框（点击顶部⭐按钮）：
  - 6大精神卡片（坚定信念、不怕牺牲、独立自主、顾全大局、紧密团结、勇往直前）
  - 时代价值板块
- 顶部导航栏添加故事/精神按钮

## 长征数字地图修复
- 完全重写了 `长征数字地图-最终版.html` 的 JavaScript 部分
- 修复问题：
  1. 事件从 `#map-viewport` 改为绑定到 `#map-container`（解决 overlay 元素吞事件的问题）
  2. SVG CSS 改为 `top:0; left:0; transform-origin: 0 0;`，完全由 JS 控制定位
  3. 添加拖拽/点击区分机制（`hasDragged` + 5px 阈值），防止拖拽误触节点点击
  4. `currentNodeId` 声明提前到 `openModal` 之前
  5. 按钮从 `onclick` 内联改为 `addEventListener`，避免全局函数依赖
  6. 添加 `user-select: none` 和 `will-change: transform` 优化
  7. 添加 Unsplash 图片作为弹窗头图（带渐变回退）
  8. 添加触摸支持（单指拖拽 + 双指缩放）
- 缩放范围：0.3x ~ 5x，支持鼠标居中缩放
